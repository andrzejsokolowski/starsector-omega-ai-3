# Fleet Trial: first Coordinate run

The user reported a defeat with every friendly ship lost after selecting Coordinate.
The log contains zero accepted Omega orders, zero rejected orders, and no proposal events through the last sample.
AI Tweaks fleet cohesion remained enabled in the captured configuration, which blocks Omega from issuing orders.
This result does not establish that Omega's coordination caused the defeat.

## Recorded evidence

The [structured record](2026-09-11-coordinate-01.json) contains the 46 periodic summaries and their source line numbers.
They cover rounded simulation times 0 through 450 seconds.
No battle-end summary appears for this run in the final capture.
The outcome comes from the user, and the total combat duration remains unverified.

| Measurement | Value |
| --- | --- |
| Build | Omega AI 0.1.1, the same build as the first Observe run |
| User-reported outcome | Enemy victory, all friendly ships lost |
| Selected mode in every periodic summary | Coordinate |
| Accepted Omega orders | 0 |
| Rejected Omega orders | 0 |
| Active Omega orders | 0 in every summary |
| Proposals and proposal events | 0 |
| Sampled unreadable observations | 0 |
| Peak planning pass | 0.40 ms |
| Last recorded simulation time | 450 seconds, rounded, without an end marker |
| Omega warnings or errors | 0 in the sampled interval |
| Other warnings and errors | 28 Combat Chatter warnings and 12 LunaLib errors |

The other messages repeat the null-faction chatter failure and missing `shaderLib` configuration value seen in the first run.
Neither those messages nor the difference in reported losses establishes a cause for the defeat.
The log does not contain ship positions, target histories, a casualty roster, or flagship autopilot state.
It cannot explain the individual engagements from these summaries alone.

## Mode selection and effective control

The settings snapshot captured at 23:44:29 UTC records Coordinate mode and enabled AI Tweaks fleet cohesion.
Omega's `Options.mayIssueOrders()` rejects order creation under that conflict.
The zero accepted-order count agrees with that guard.
The old summary prints the requested mode without printing the effective permission or conflict.
That omission makes a blocked run look like a coordination test.

The zero proposal count remains a separate unresolved issue.
Observation can continue during a global conflict, while per-ship controllers and assignments still restrict eligibility.
Build 0.1.1 does not record those exclusions.
Local build 0.1.2 adds that information and identifies blocked mode explicitly.

## Comparison with the first run

The [Observe run](2026-09-11-observe-01.md) ended in a reported player victory with one Wolf lost.
Both runs recorded no Omega orders.
Battle variation, player control, existing AI, and differences in actual engagements remain possible explanations for the different outcomes.
The available evidence does not rank those explanations.

For the next pair, disable AI Tweaks fleet cohesion for both Observe and Coordinate.
Keep the rest of the setup and flagship control consistent.
Use the decision labels to establish that eligible ships exist and that an Omega order actually takes effect.
Record that behavior separately from the battle result.

The full log and configuration snapshots remain gitignored under `.private/test-runs/`.
No game installation or user configuration changed during this analysis.
